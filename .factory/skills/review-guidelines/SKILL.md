# Review Guidelines

When in doubt, report the finding. It is far better to flag a potential issue that turns out to be benign than to miss a real bug. False negatives are much more costly than false positives in code review.

Do not skip a file just because the change looks simple. Do not skip a finding just because you are unsure. If there is a reasonable chance the code is wrong, report it.
